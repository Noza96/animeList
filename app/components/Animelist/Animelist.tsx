import { defaultHead } from "next/head";
import style from "./Animelist.module.scss";

const Animelist = () => {
  return (
    <div>
      <ul className={}>
        <li>item</li>
      </ul>
    </div>
  );
};

export default Animelist;
